import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Set the document title to the roll number
document.title = "RA2111026010207";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);